.side-bar
  .header
    .side__bar__menu
      .header__name
        =current_user.name
      %ul.edit__buttonm 
        %li.list
          = link_to '#' do
            = icon('fas', 'edit', class: 'icon')
        %li.list
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')

  .groups
    .group
      .group__name
        グループ名
      .group__message
        メッセージ
